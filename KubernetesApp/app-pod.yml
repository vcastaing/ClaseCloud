apiVersion: v1
kind: Pod
metadata:
    name: app-pod
    labels:
        app: app
        type: mvc
spec:
  containers:
     - name: app-pod
       image: vcastaing/busybox.v1
       ports:
        - containerPort: 8080